# algorithm -a is an optional string
#algorithm: bwtsw
   #   BWT construction algorithm: bwtsw or is (Default: auto)
reference_genome:
  class: File
  path: /mnt/data/input/hs37d5.fa
# -b default 10000000
block_size: 100000000
  #    Block size for the bwtsw algorithm (effective with -a bwtsw) (Default: 10000000)

